import React from 'react';
import Nav from './Nav';

const Header = (props) => {
    return(
        <div>
            <h1>React Gallery App</h1>
            <nav reset={props.reset} />
        </div>
    )
};

export default Header;
//app title

//logo

//footer

//nav

// search bar







